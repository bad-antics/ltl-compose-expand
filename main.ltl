// ltl-compose-expand/main.ltl
//
// docker-compose v3 expander — env vars + extends.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-compose-expand v0.1.0")
    io::println("docker-compose v3 expander — env vars + extends.")
    0
}
