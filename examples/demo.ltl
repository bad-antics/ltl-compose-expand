// examples/demo.ltl — runnable demo for ltl_compose_expand

import std::io
use ltl_compose_expand as lib

fn main() {
    io::println("ltl_compose_expand demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}